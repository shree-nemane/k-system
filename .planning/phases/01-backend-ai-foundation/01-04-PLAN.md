---
plan: "01-04"
phase: "1"
wave: 2
depends_on: ["01-01", "01-02"]
requirements_addressed: [CRWD-01, CRWD-02, BKND-01, BKND-02]
files_modified:
  - app/schemas/crowd.py
  - app/schemas/sos.py
  - app/schemas/alert.py
  - app/schemas/camera.py
  - app/ws/manager.py
  - app/routers/crowd.py
  - app/routers/sos.py
  - app/routers/alerts.py
  - app/routers/cameras.py
  - main.py
autonomous: true
---

# Plan 01-04: API Endpoints & WebSocket Broadcast

## Objective
Replace all `{"status": "not_implemented"}` placeholder routes (from Plan 01-01) with fully functional endpoints. Implement all 5 REST endpoints + WebSocket connection manager. The `POST /crowd/update` endpoint must atomically write to PostgreSQL AND broadcast over WebSocket to all connected mobile clients (D-09).

## Tasks

<task id="T01">
  <title>Define Pydantic v2 Request/Response Schemas</title>
  <read_first>
    - .planning/phases/01-backend-ai-foundation/01-CONTEXT.md — D-14 (exact endpoint contracts), D-09 (WebSocket payload format: type="density_update" or "alert")
    - .planning/phases/01-backend-ai-foundation/01-CONTEXT.md — Specifics section: WebSocket JSON type field, SOS UUID confirmation, alert JSON format
    - backend/app/models/crowd_event.py — column types to match in schema
    - backend/app/models/sos_request.py — UUID PK, status field
    - backend/app/models/alert.py — event_type, severity, camera_id
    - backend/app/models/camera.py — all camera fields
  </read_first>
  <action>
    Create `backend/app/schemas/crowd.py`:
    ```python
    from pydantic import BaseModel
    from datetime import datetime

    class CrowdUpdateRequest(BaseModel):
        camera_id: str
        person_count: int
        density_level: str           # 'low', 'medium', 'high'
        trigger_alert: bool = False
        alert_threshold: int | None = None

    class CrowdStatusItem(BaseModel):
        camera_id: str
        person_count: int
        density_level: str
        timestamp: datetime

        model_config = {"from_attributes": True}

    class CrowdStatusResponse(BaseModel):
        cameras: list[CrowdStatusItem]
    ```

    Create `backend/app/schemas/sos.py`:
    ```python
    import uuid
    from pydantic import BaseModel

    class SOSCreateRequest(BaseModel):
        latitude: float
        longitude: float
        device_id: str | None = None

    class SOSCreateResponse(BaseModel):
        id: uuid.UUID
        status: str
        message: str = "SOS received"

        model_config = {"from_attributes": True}
    ```

    Create `backend/app/schemas/alert.py`:
    ```python
    from datetime import datetime
    from pydantic import BaseModel

    class AlertRead(BaseModel):
        id: int
        event_type: str
        camera_id: str
        severity: str
        person_count: int | None
        threshold: int | None
        fired_at: datetime

        model_config = {"from_attributes": True}

    class AlertsResponse(BaseModel):
        alerts: list[AlertRead]
        total: int
    ```

    Create `backend/app/schemas/camera.py`:
    ```python
    from pydantic import BaseModel

    class CameraRead(BaseModel):
        camera_id: str
        name: str
        location_label: str | None
        is_active: bool
        low_thresh: int
        high_thresh: int

        model_config = {"from_attributes": True}

    class CamerasResponse(BaseModel):
        cameras: list[CameraRead]
    ```
  </action>
  <acceptance_criteria>
    - `backend/app/schemas/crowd.py` contains `class CrowdUpdateRequest(BaseModel)`
    - `backend/app/schemas/crowd.py` contains `trigger_alert: bool = False`
    - `backend/app/schemas/sos.py` contains `class SOSCreateResponse(BaseModel)`
    - `backend/app/schemas/sos.py` contains `id: uuid.UUID`
    - `backend/app/schemas/alert.py` contains `class AlertRead(BaseModel)`
    - `backend/app/schemas/alert.py` contains `fired_at: datetime`
    - `backend/app/schemas/camera.py` contains `class CameraRead(BaseModel)`
    - `backend/app/schemas/camera.py` contains `low_thresh: int` and `high_thresh: int`
  </acceptance_criteria>
</task>

<task id="T02">
  <title>Implement WebSocket ConnectionManager</title>
  <read_first>
    - .planning/phases/01-backend-ai-foundation/01-CONTEXT.md — D-14 (WS /ws/crowd sends density_update AND alert types)
    - .planning/phases/01-backend-ai-foundation/01-RESEARCH.md — ConnectionManager pattern: connect/disconnect/broadcast with try/except for dead connection cleanup
  </read_first>
  <action>
    Create `backend/app/ws/manager.py`:
    ```python
    import json
    from fastapi import WebSocket
    from typing import Any


    class ConnectionManager:
        """
        Manages active WebSocket connections.
        Thread-safe for single-process FastAPI (not multi-instance).
        For multi-instance scale: replace with Redis pub/sub.
        """

        def __init__(self):
            self.active_connections: list[WebSocket] = []

        async def connect(self, websocket: WebSocket) -> None:
            await websocket.accept()
            self.active_connections.append(websocket)
            print(f"[WS] Client connected. Total: {len(self.active_connections)}")

        def disconnect(self, websocket: WebSocket) -> None:
            if websocket in self.active_connections:
                self.active_connections.remove(websocket)
            print(f"[WS] Client disconnected. Total: {len(self.active_connections)}")

        async def broadcast(self, data: dict[str, Any]) -> None:
            """
            Broadcast JSON payload to all connected clients.
            Silently removes dead connections.
            data MUST have 'type' key: 'density_update' or 'alert' (D-14/Specifics)
            """
            message = json.dumps(data, default=str)
            dead: list[WebSocket] = []
            for connection in self.active_connections:
                try:
                    await connection.send_text(message)
                except Exception:
                    dead.append(connection)
            for conn in dead:
                self.disconnect(conn)

        async def send_personal(self, websocket: WebSocket, data: dict[str, Any]) -> None:
            """Send to a specific client only."""
            message = json.dumps(data, default=str)
            await websocket.send_text(message)


    # Singleton instance shared across routers
    ws_manager = ConnectionManager()
    ```
  </action>
  <acceptance_criteria>
    - `backend/app/ws/manager.py` contains `class ConnectionManager:`
    - `backend/app/ws/manager.py` contains `async def connect(self, websocket: WebSocket)`
    - `backend/app/ws/manager.py` contains `async def broadcast(self, data: dict`
    - `backend/app/ws/manager.py` contains `try:` and `dead.append(connection)` pattern in broadcast
    - `backend/app/ws/manager.py` contains `ws_manager = ConnectionManager()`
    - `backend/app/ws/manager.py` contains `json.dumps(data, default=str)`
  </acceptance_criteria>
</task>

<task id="T03">
  <title>Implement POST /crowd/update & GET /crowd/status Endpoints</title>
  <read_first>
    - .planning/phases/01-backend-ai-foundation/01-CONTEXT.md — D-09 (POST saves to DB AND broadcasts WS), D-14 (endpoint paths and payloads)
    - backend/app/schemas/crowd.py — CrowdUpdateRequest, CrowdStatusResponse
    - backend/app/models/crowd_event.py — CrowdEvent ORM model columns
    - backend/app/models/alert.py — Alert ORM model columns
    - backend/app/ws/manager.py — ws_manager.broadcast() signature
    - backend/app/db/session.py — get_db dependency
    - backend/app/core/security.py — verify_api_key dependency
  </read_first>
  <action>
    Replace `backend/app/routers/crowd.py` with full implementation:
    ```python
    from datetime import datetime, timezone
    from fastapi import APIRouter, Depends
    from sqlalchemy import select, func
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.security import verify_api_key
    from app.db.session import get_db
    from app.models.crowd_event import CrowdEvent
    from app.models.alert import Alert
    from app.schemas.crowd import CrowdUpdateRequest, CrowdStatusResponse, CrowdStatusItem
    from app.ws.manager import ws_manager

    router = APIRouter(prefix="/crowd", tags=["crowd"])


    @router.post("/update", dependencies=[Depends(verify_api_key)])
    async def update_crowd(
        payload: CrowdUpdateRequest,
        db: AsyncSession = Depends(get_db),
    ):
        """
        Receives crowd snapshot from AI worker.
        1. Saves CrowdEvent to DB.
        2. If trigger_alert: saves Alert to DB.
        3. Broadcasts density_update (and alert if triggered) over WebSocket.
        """
        # 1. Save crowd event
        event = CrowdEvent(
            camera_id=payload.camera_id,
            person_count=payload.person_count,
            density_level=payload.density_level,
        )
        db.add(event)

        alert_data = None
        # 2. Create alert record if triggered (D-08)
        if payload.trigger_alert:
            alert = Alert(
                event_type="overcrowd",
                camera_id=payload.camera_id,
                severity="high",
                person_count=payload.person_count,
                threshold=payload.alert_threshold,
            )
            db.add(alert)
            alert_data = {
                "type": "alert",
                "event": "overcrowd",
                "camera_id": payload.camera_id,
                "severity": "high",
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "data": {
                    "person_count": payload.person_count,
                    "threshold": payload.alert_threshold,
                },
            }

        await db.commit()

        # 3. Broadcast density update over WebSocket (D-09)
        await ws_manager.broadcast({
            "type": "density_update",
            "camera_id": payload.camera_id,
            "person_count": payload.person_count,
            "density_level": payload.density_level,
            "timestamp": datetime.now(timezone.utc).isoformat(),
        })

        if alert_data:
            await ws_manager.broadcast(alert_data)

        return {"status": "ok", "alert_triggered": payload.trigger_alert}


    @router.get("/status", dependencies=[Depends(verify_api_key)], response_model=CrowdStatusResponse)
    async def get_crowd_status(db: AsyncSession = Depends(get_db)):
        """Return the most recent crowd event per camera."""
        # Subquery: max timestamp per camera_id
        subq = (
            select(CrowdEvent.camera_id, func.max(CrowdEvent.timestamp).label("max_ts"))
            .group_by(CrowdEvent.camera_id)
            .subquery()
        )
        stmt = select(CrowdEvent).join(
            subq,
            (CrowdEvent.camera_id == subq.c.camera_id)
            & (CrowdEvent.timestamp == subq.c.max_ts),
        )
        result = await db.execute(stmt)
        events = result.scalars().all()
        return CrowdStatusResponse(cameras=[CrowdStatusItem.model_validate(e) for e in events])
    ```
  </action>
  <acceptance_criteria>
    - `backend/app/routers/crowd.py` contains `@router.post("/update"`
    - `backend/app/routers/crowd.py` contains `@router.get("/status"`
    - `backend/app/routers/crowd.py` contains `await ws_manager.broadcast(`
    - `backend/app/routers/crowd.py` contains `"type": "density_update"`
    - `backend/app/routers/crowd.py` contains `"type": "alert"`
    - `backend/app/routers/crowd.py` contains `db.add(event)` and `db.add(alert)`
    - `backend/app/routers/crowd.py` contains `await db.commit()`
    - `backend/app/routers/crowd.py` contains `func.max(CrowdEvent.timestamp)` for latest-per-camera query
  </acceptance_criteria>
</task>

<task id="T04">
  <title>Implement POST /sos/request, GET /alerts/recent, GET /cameras & WS /ws/crowd</title>
  <read_first>
    - .planning/phases/01-backend-ai-foundation/01-CONTEXT.md — D-14 (all endpoint specs), Specifics: SOS returns UUID confirmation
    - backend/app/schemas/sos.py — SOSCreateRequest, SOSCreateResponse
    - backend/app/schemas/alert.py — AlertRead, AlertsResponse
    - backend/app/schemas/camera.py — CameraRead, CamerasResponse
    - backend/app/models/sos_request.py — SOSRequest ORM model
    - backend/app/models/alert.py — Alert ORM model
    - backend/app/models/camera.py — Camera ORM model
    - backend/app/ws/manager.py — ws_manager, ConnectionManager
    - main.py — where WS route must be registered
  </read_first>
  <action>
    Replace `backend/app/routers/sos.py`:
    ```python
    from fastapi import APIRouter, Depends
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.security import verify_api_key
    from app.db.session import get_db
    from app.models.sos_request import SOSRequest
    from app.schemas.sos import SOSCreateRequest, SOSCreateResponse

    router = APIRouter(prefix="/sos", tags=["sos"])

    @router.post("/request", dependencies=[Depends(verify_api_key)], response_model=SOSCreateResponse)
    async def submit_sos(payload: SOSCreateRequest, db: AsyncSession = Depends(get_db)):
        """
        Accept SOS from mobile app. Returns UUID for retry deduplication. (D-14 Specifics)
        """
        sos = SOSRequest(
            latitude=payload.latitude,
            longitude=payload.longitude,
            device_id=payload.device_id,
            status="pending",
        )
        db.add(sos)
        await db.commit()
        await db.refresh(sos)
        return SOSCreateResponse(id=sos.id, status=sos.status)
    ```

    Replace `backend/app/routers/alerts.py`:
    ```python
    from datetime import datetime, timedelta, timezone
    from fastapi import APIRouter, Depends, Query
    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.security import verify_api_key
    from app.db.session import get_db
    from app.models.alert import Alert
    from app.schemas.alert import AlertsResponse, AlertRead

    router = APIRouter(prefix="/alerts", tags=["alerts"])

    @router.get("/recent", dependencies=[Depends(verify_api_key)], response_model=AlertsResponse)
    async def get_recent_alerts(
        minutes: int = Query(default=15, ge=1, le=60),
        db: AsyncSession = Depends(get_db),
    ):
        """Return alerts from the last N minutes (default 15). Mobile fallback polling."""
        since = datetime.now(timezone.utc) - timedelta(minutes=minutes)
        stmt = select(Alert).where(Alert.fired_at >= since).order_by(Alert.fired_at.desc())
        result = await db.execute(stmt)
        alerts = result.scalars().all()
        return AlertsResponse(
            alerts=[AlertRead.model_validate(a) for a in alerts],
            total=len(alerts),
        )
    ```

    Replace `backend/app/routers/cameras.py`:
    ```python
    from fastapi import APIRouter, Depends
    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.security import verify_api_key
    from app.db.session import get_db
    from app.models.camera import Camera
    from app.schemas.camera import CamerasResponse, CameraRead

    router = APIRouter(prefix="/cameras", tags=["cameras"])

    @router.get("/", dependencies=[Depends(verify_api_key)], response_model=CamerasResponse)
    async def list_cameras(db: AsyncSession = Depends(get_db)):
        """Return all configured cameras. Mobile uses this to build the map overlay."""
        stmt = select(Camera).where(Camera.is_active == True)
        result = await db.execute(stmt)
        cameras = result.scalars().all()
        return CamerasResponse(cameras=[CameraRead.model_validate(c) for c in cameras])
    ```

    Update `backend/main.py` to add WebSocket endpoint:
    ```python
    # Add these imports at top:
    from fastapi import WebSocket, WebSocketDisconnect
    from app.ws.manager import ws_manager

    # Add this route inside main.py (after include_router calls):
    @app.websocket("/ws/crowd")
    async def websocket_crowd(websocket: WebSocket):
        """
        WebSocket endpoint for live crowd density and alert push.
        Payload types: {"type": "density_update", ...} or {"type": "alert", ...}
        No auth required for WS (mobile connects before auth headers available).
        """
        await ws_manager.connect(websocket)
        try:
            while True:
                # Keep connection alive; server pushes data via broadcast()
                await websocket.receive_text()
        except WebSocketDisconnect:
            ws_manager.disconnect(websocket)
    ```
  </action>
  <acceptance_criteria>
    - `backend/app/routers/sos.py` contains `async def submit_sos(`
    - `backend/app/routers/sos.py` contains `return SOSCreateResponse(id=sos.id`
    - `backend/app/routers/alerts.py` contains `minutes: int = Query(default=15`
    - `backend/app/routers/alerts.py` contains `timedelta(minutes=minutes)`
    - `backend/app/routers/cameras.py` contains `Camera.is_active == True`
    - `backend/main.py` contains `@app.websocket("/ws/crowd")`
    - `backend/main.py` contains `await ws_manager.connect(websocket)`
    - `backend/main.py` contains `except WebSocketDisconnect:`
    - `curl http://localhost:8000/docs` shows all 5 REST endpoints in Swagger UI
  </acceptance_criteria>
</task>

## Verification
1. `uvicorn main:app --reload` starts and `/docs` shows all 5 endpoints + WS at `/ws/crowd`
2. `curl -X POST http://localhost:8000/crowd/update -H "X-API-Key: $API_KEY" -H "Content-Type: application/json" -d '{"camera_id":"cam_01","person_count":80,"density_level":"high","trigger_alert":true,"alert_threshold":75}'` returns `{"status":"ok","alert_triggered":true}`
3. `curl http://localhost:8000/crowd/status -H "X-API-Key: $API_KEY"` returns camera list with density levels
4. `curl -X POST http://localhost:8000/sos/request -H "X-API-Key: $API_KEY" -H "Content-Type: application/json" -d '{"latitude":25.4358,"longitude":81.8463}'` returns `{"id":"<uuid>","status":"pending","message":"SOS received"}`
5. `curl http://localhost:8000/alerts/recent -H "X-API-Key: $API_KEY"` returns alerts list
6. `curl http://localhost:8000/cameras/ -H "X-API-Key: $API_KEY"` returns cameras list
7. Connect to `ws://localhost:8000/ws/crowd` with `wscat -c ws://localhost:8000/ws/crowd`; then POST to `/crowd/update` — WebSocket client receives the broadcast JSON

## must_haves
- `POST /crowd/update` MUST perform DB write AND WebSocket broadcast atomically (commit first, then broadcast)
- `WS /ws/crowd` MUST NOT require X-API-Key (mobile connects before auth is established)
- All REST endpoints MUST return proper HTTP status codes (201 for POST /sos/request, 200 for GETs)
- `GET /alerts/recent` MUST support `?minutes=N` query parameter for flexible polling windows
