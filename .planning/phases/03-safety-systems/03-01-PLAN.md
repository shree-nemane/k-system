# Plan 03-01: SOS Identity & Local Persistence

**Wave:** 1
**Depends on:** Phase 2 Complete
**Autonomous:** true
**Requirements:** [SAFE-02, SAFE-03]
**Files Modified:**
- mobile/lib/core/identity.dart [NEW]
- mobile/lib/models/sos_request.dart [NEW]
- mobile/lib/models/emergency_contact.dart [NEW]
- mobile/lib/providers/identity_provider.dart [NEW]
- mobile/lib/providers/sos_queue_provider.dart [NEW]
- mobile/lib/screens/safety_settings_screen.dart [NEW]
- mobile/pubspec.yaml
- backend/app/models/user.py [NEW]
- backend/app/routers/users.py [NEW]

## Objective
Establish the foundational identity and local persistence layers required for reliable emergency signaling. Use Isar to create a safe "outbox" for SOS pulses and persistent Device IDs to link mobile clients to the backend.

## Tasks

<task>
<read_first>
- mobile/pubspec.yaml
- mobile/lib/screens/sos_screen.dart
</read_first>
<action>
Add `isar`, `isar_flutter_libs`, and `path_provider` to `mobile/pubspec.yaml`.
Add `isar_generator` and `build_runner` to `dev_dependencies`.
</action>
<acceptance_criteria>
`mobile/pubspec.yaml` contains `isar: ^3.1.0` (or latest stable) and `dev_dependencies` includes `isar_generator`.
</acceptance_criteria>
</task>

<task>
<read_first>
- mobile/lib/main.dart
</read_first>
<action>
Define Isar schemas for `SOSRequest` and `EmergencyContact`.
- `SOSRequest`: id (AutoIncrement), uuid (String), latitude, longitude, altitude, batteryLevel, category, status (pending/sent), createdAt.
- `EmergencyContact`: id (Fixed 1), name, phone, relationship.
Run `flutter pub run build_runner build` to generate schemas.
</action>
<acceptance_criteria>
`mobile/lib/models/sos_request.g.dart` and `mobile/lib/models/emergency_contact.g.dart` are generated successfully.
</acceptance_criteria>
</task>

<task>
<read_first>
- mobile/lib/main.dart
</read_first>
<action>
Create `mobile/lib/core/identity.dart` to handle Persistent Device ID.
- Check `shared_preferences` for existing `device_id`.
- If missing, generate `Uuid().v4()` and persist it.
- Export `getDeviceId()` helper.
</action>
<acceptance_criteria>
`mobile/lib/core/identity.dart` exists and provides a stable UUID string that persists across app restarts.
</acceptance_criteria>
</task>

<task>
<read_first>
- backend/app/models/sos_request.py
</read_first>
<action>
Update backend `users` table and API:
- Create `backend/app/models/user.py` with `device_id` (PK), `push_token`, and `emergency_contact_json`.
- Create `backend/app/routers/users.py` with `POST /users/register` (upsert based on `device_id`).
</action>
<acceptance_criteria>
Backend has a `User` model and a `/users/register` endpoint. `alembic upgrade head` completes.
</acceptance_criteria>
</task>

<task>
<read_first>
- mobile/lib/screens/sos_screen.dart
</read_first>
<action>
Create `mobile/lib/screens/safety_settings_screen.dart`.
- Simple form to enter "Emergency Contact" (Name, Phone).
- Persist to Isar `EmergencyContact` collection.
- Link this screen from the `SOSScreen` or a "Safety" tab.
</action>
<acceptance_criteria>
User can save contact info in the app, and it persists to the local Isar database.
</acceptance_criteria>
</task>

## Verification Criteria
### Automated
- `flutter test` for Isar CRUD operations in `identity_provider`.
- `pytest` for `/users/register` endpoint in backend.

### Manual
- Reset app data, verify a new Device ID is generated.
- Save an emergency contact, close/reopen app, verify it persists.
- Trigger a mock SOS (logic in Plan 2), verify it appears in Isar with `status: pending`.
